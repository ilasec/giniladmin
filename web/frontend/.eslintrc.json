{
  "extends": ["next/core-web-vitals", "next/typescript"],
  "rules": {
    "@typescript-eslint/no-unused-vars": "warn",
    "@typescript-eslint/no-explicit-any": "warn",  // 将错误降级为警告
    "prefer-const": "warn",  // 将错误降级为警告
    "react/no-unescaped-entities": "off"  // 关闭引号转义规则
  }
}
